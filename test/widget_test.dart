import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Apenas um teste vazio para não dar erro', (WidgetTester tester) async {
    // Teste limpo que não quebra o projeto
    expect(true, true);
  });
}